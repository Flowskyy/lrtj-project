# Database
DATABASE_URL="mysql://user:password@localhost:3306/database"

# App URL (used for generating signup links in emails)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# SMTP Configuration for Email Service (Nodemailer)
# Fill these with real SMTP credentials to enable actual email delivery
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="cnz191128@gmail.com"
SMTP_PASSWORD="ucdx aycw xmfj qkas"
SMTP_FROM="noreply@lrtj.co.id"
SMTP_FROM_NAME="LRT Jakarta"

# If SMTP is not configured, emails will be logged to console instead
